#include<stdio.h>
#include<stdlib.h>


int main()
{
    int Arr[5];         //Static memort allocation
    int size = 0;

    printf("Enter number of elements that you want to allocate: ");
    scanf("%d", %size);

    return 0;
}